# TEMPORAL — Prueba de que el CI es bloqueante (issue #4, criterio de verificación).
# Este import no se usa a propósito: ruff lo marca como F401 y el CI debe fallar.
# BORRAR este archivo tras confirmar que el PR queda bloqueado.
import os
